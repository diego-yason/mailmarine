import { InteractionJson } from "res/types/interaction";

export default <InteractionJson>{
    name: "unassign",
    description: "Unassigns your server's CSMS channel",
    default_permission: false
}